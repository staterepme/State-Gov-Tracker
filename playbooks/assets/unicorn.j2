description "Gunicorn Upstart Script for staterep"

start on runlevel [2345]
stop on runlevel [!2345]

kill timeout 5

chdir {{ app_dir }}/

exec {{ unicorn_exec }} -w 2 --log-file "{{ unicorn_log }}" -b "0.0.0.0:{{ unicorn_port }}" staterep:application